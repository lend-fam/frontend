global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    cluster: '${CLUSTER_NAME:-lend-fam}'
    environment: '${ENVIRONMENT:-production}'
    region: '${REGION:-unknown}'

# Grafana Cloud remote write configuration
remote_write:
  - url: '${GRAFANA_CLOUD_PROMETHEUS_URL}/api/prom/push'
    basic_auth:
      username: '${GRAFANA_CLOUD_PROMETHEUS_USERNAME}'
      password: '${GRAFANA_CLOUD_API_KEY}'
    queue_config:
      capacity: 10000
      max_samples_per_send: 2000
      batch_send_deadline: 5s
      min_backoff: 30ms
      max_backoff: 5s
      retry_on_http_429: true
    # Filter metrics to reduce costs - allow essential metrics
    write_relabel_configs:
      # Allow basic infrastructure metrics
      - source_labels: [__name__]
        regex: '(up|node_.*|container_memory_usage_bytes|container_cpu_usage_seconds_total|container_network_receive_bytes_total|container_network_transmit_bytes_total|container_last_seen|container_start_time_seconds|prometheus_.*)'
        action: keep
      # Add readable service names for containers
      - source_labels: [container_label_com_docker_compose_service]
        regex: '(.+)'
        target_label: service
        replacement: '${1}'
      - source_labels: [name]
        regex: '(.+)'
        target_label: container_name
        replacement: '${1}'

scrape_configs:
  # Prometheus itself
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
    scrape_interval: 30s
    metrics_path: /metrics

  # Node Exporter - System metrics
  - job_name: 'node-exporter'
    static_configs:
      - targets: ['node-exporter:9100']
        labels:
          node: 'lend-fam-server'
    scrape_interval: 15s
    metrics_path: /metrics

  # cAdvisor - Container metrics
  - job_name: 'cadvisor'
    static_configs:
      - targets: ['cadvisor:8080']
    scrape_interval: 15s
    metrics_path: /metrics
    metric_relabel_configs:
      # Add readable service names from compose labels
      - source_labels: [container_label_com_docker_compose_service]
        target_label: service
        regex: '(.+)'
        replacement: '${1}'
      # Add container name as label
      - source_labels: [name]
        target_label: container_name
        regex: '(.+)'
        replacement: '${1}'
      # Drop metrics for root container to reduce noise
      - source_labels: [id]
        regex: '/'
        action: drop

  # Frontend application health check
  - job_name: 'frontend-health'
    static_configs:
      - targets: ['172.17.0.1:3000']  # Docker host IP
    scrape_interval: 30s
    metrics_path: /health
    scrape_timeout: 10s
    scheme: http
    metric_relabel_configs:
      - target_label: service
        replacement: 'frontend-app'

# Rule files for alerting (optional)
rule_files:
  # - "rules/*.yml"

# Basic alerting configuration (can be extended)
alerting:
  alertmanagers:
    - static_configs:
        - targets: []